import React from 'react';
import PropTypes from 'prop-types';

const StockFollows = props => {
    return (
        <div>
            
        </div>
    );
};

StockFollows.propTypes = {
    stocks: PropTypes.array.isRequired,
    
};

export default StockFollows;